import React from 'react';
import Form from './Form';
import UserData from './UserData';

const Home = () => {
    return (
        <div>
            <UserData></UserData>
            <Form></Form>
            
        </div>
    );
};

export default Home;